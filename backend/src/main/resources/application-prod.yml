spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DB}?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

rocketmq:
  name-server: ${ROCKETMQ_NAMESRV}

logging:
  level:
    com.seckill: info
